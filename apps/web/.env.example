# Public (safe to expose to the browser). Get them from Supabase → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://tgqwyheqhquwncyloond.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
# Public URL of the web app (used for auth redirects and invite links).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# NEVER put the service_role key in this app.
